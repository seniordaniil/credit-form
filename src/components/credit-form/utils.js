export const years = [];

for (let i = 1; i < 101; i++) {
  years.push({
    value: i,
    label: i,
  });
}

export const months = [];

for (let i = 1; i < 13; i++) {
  months.push({
    value: i,
    label: i,
  });
}
